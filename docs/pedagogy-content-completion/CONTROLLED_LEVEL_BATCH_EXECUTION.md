# Controlled delegated primary level review

Scope: only the twelve explicitly authorized primary level attachments. No adoption, teaching declaration, timetable, coefficients, assignment, preschool or secondary mutation. No provider calls.

Execution plan:
1. Pin the exact private authorization manifest by its SHA-256 in the IAM-authenticated admin executable. No manifest/tenant identifiers are published.
2. Test dry-run, all-or-nothing transaction, versions, tenant/year, concurrency, truthful system attribution, consumed receipt, and immutable multi-component evidence on Linux emulators.
3. Pass existing Rules, Functions and Lots A/B/C/D gates; merge normally to staging and verify the deployed SHA.
4. Run the pinned executable with `--dry-run`, then `--apply-approved-twelve`. The latter repeats dry-run and checks the snapshot inside its transaction.
5. Reload all twelve current records, history and audits. The consumed receipt makes retries read-only and rejects later modifications. No public endpoint or additional IAM grant is introduced.
6. Keep safe mappings and documentary relations as private prepared manifests, not approvals. Retain the seven unanswered ITALO questions.

Manual owner callables and client Rules remain unchanged. The operator uses existing IAM credentials, not an owner session. Records explicitly distinguish the owner's authorization from the system recorder. A SHA-256 pin is a controlled allowlist, not a claim of cryptographic signature by the owner.

The evidence array records the complete versioned documentary snapshot separately from the selected human decision. Components remain components, not EXACT equivalences. Immutable revision history and evidence checksum prevent a later local-subject choice from erasing documentary sibling relations.

Deployment and execution evidence: pending; do not infer PASS from this plan. Actual private manifest and delivery evidence must stay outside tracked/public files.
