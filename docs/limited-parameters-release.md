# Limited Parameters release

Base: e1f236ae5c370e9de5ccf9efaa99d53483b9b1a4. Extracted functional source: 99c2daacd8ead63c9337456ea72c81bfe4f822b5, which must not itself be promoted.

Only Settings, fee targeting/catalogue, preservation of frozen obligations, tuition deadline validation and student transport/class fee presentation change. Academic year and periods reuse the existing Production implementation. No new curriculum, planning, grading, report-card or pedagogy implementation is included. Payments.tsx is deliberately retained from Production, including its tuition recovery behavior and tests. Existing class and subject management remain accessible from Settings through the existing canonical pages.

All existing Production workflows, safety scripts and security contracts remain intact. The Production manifest test is strengthened by requiring updateAcademicYearBounds in addition to every previously required endpoint. The generic Production workflow gains only updateAcademicYearBounds, already exported and required by Settings. Its existing broader deployment contract is retained for compatibility, not used by this release precheck. No existing gate is removed or reduced.

scripts/limited-parameters-functions.json is the exhaustive scoped manifest for this limited release promotion and rollback procedure. It contains only Settings/calendar and financial/collection dependencies. The isolated Staging workflow deploys this manifest and Firestore Rules, not all exports, then verifies all endpoints ACTIVE and checks the immutable frontend SHA receipt. Storage Rules, indexes and legacy academic endpoints are unchanged. The global generic main workflow must not be triggered as a substitute for this scoped promotion: this precheck neither merges main nor deploys Production.

Validation uses synthetic schools/users on ecoscolaire-staging only. Global Settings smoke creates no payments; the collection smoke creates and reverses only synthetic cash collections to validate partial payments, multiple fees, receipts, advantages, moratoria and idempotency, then removes its fixtures. No external payment provider is invoked. Cleanup assertions run in finally.

Before any authorized promotion: obtain a successful full Firestore export less than 24 hours old; verify immutable frontend and backend source SHA; retain this manifest, Rules and source from the rollback target e1f236ae5c370e9de5ccf9efaa99d53483b9b1a4. Scope the backend promotion to the manifest and firestore:rules, guarded by the backup checker and fail-closed deployment wrapper. Never execute a data import as an automatic rollback. Restoring code does not justify recalculating debts or rewriting financial ledgers. Frontend rollback target is the existing immutable Vercel Production deployment ecoscolaire-7nbuceirl-linda-lemofouet-s-projects.vercel.app. No rollback is executed during precheck.

The additional limited unit gate selects the required Settings/financial/transport suites and runs them serially. Existing generic CI is unchanged. A broader local audit also ran the legacy grading tests and exposed 11 failures in two unchanged grading test files (missing current calendar/program fixture setup); these are outside this release and are neither rewritten nor removed. The new gate does not replace any existing CI step. Generic lint keeps its pre-existing warning policy; changed scope additionally requires zero warnings.
